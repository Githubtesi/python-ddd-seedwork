from abc import ABC

class DomainService(ABC):
    """
    ドメインサービスの基底クラス。
    ドメインオブジェクト（Entity/ValueObject）に収まらない
    複数のオブジェクトにまたがる計算や、重複チェックなどのロジックを記述します。
    
    例: 
    - 銀行振込（送り手と受け手の両方の操作が必要な場合）
    - ユーザー名の重複チェック（リポジトリへの問い合わせが必要な場合）
    """
    pass
